{% extends 'base.html.twig' %}

{% block title %}Versement{% endblock %}

{% block body %}
    <h1>Versement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ versement.id }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ versement.montant }}</td>
            </tr>
            <tr>
                <th>Acompte</th>
                <td>{{ versement.acompte }}</td>
            </tr>
            <tr>
                <th>Reste</th>
                <td>{{ versement.reste }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ versement.date }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ versement.createdAt ? versement.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_versement_index') }}">back to list</a>

    <a href="{{ path('app_versement_edit', {'id': versement.id}) }}">edit</a>

    {{ include('versement/_delete_form.html.twig') }}
{% endblock %}
