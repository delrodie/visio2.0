<form method="post" action="{{ path('assurance_delete', {'id': assurance.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette assurance?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ assurance.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
